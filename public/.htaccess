<IfModule mod_rewrite.c>
  RewriteEngine On

  # Send requests to index.html if not a real file or directory
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
